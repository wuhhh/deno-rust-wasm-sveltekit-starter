{
  "tasks": {
    "wasmbuild": "deno run -A jsr:@deno/wasmbuild@0.19.0",
    "www-dev": "deno task --cwd=www dev",
    "www-build": "deno task --cwd=www build",
    "www-preview": "deno task --cwd=www preview",
    "www-prepare": "deno task --cwd=www prepare",
    "www-check": "deno task --cwd=www check",
    "www-check:watch": "deno task --cwd=www check:watch",
    "www-format": "deno task --cwd=www format",
    "www-lint": "deno task --cwd=www lint"
  }
}
